{
  "name": "cypress-1",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "open": "npx cypress open",
    "test.local": "npx cypress run",
    "test.local.headfull": "npx cypress run --headed",
    "test.sauce.ex1.eu": "CURRENT_TIME=$(date +%s) saucectl run -c ./.sauce/config-ex1.yml ",
    "test.sauce.ex1.us": "CURRENT_TIME=$(date +%s) saucectl run -c ./.sauce/config-ex1.yml --region us-west-1",
    "test.sauce.ex2.eu": "CURRENT_TIME=$(date +%s) saucectl run -c ./.sauce/config-ex2.yml ",
    "test.sauce.ex2.us": "CURRENT_TIME=$(date +%s) saucectl run -c ./.sauce/config-ex2.yml --region us-west-1",
    "test.sauce.ex3.eu": "CURRENT_TIME=$(date +%s) saucectl run -c ./.sauce/config-ex3.yml ",
    "test.sauce.ex3.us": "CURRENT_TIME=$(date +%s) saucectl run -c ./.sauce/config-ex3.yml --region us-west-1",
    "test.sauce.ex4.eu": "CURRENT_TIME=$(date +%s) saucectl run -c ./.sauce/config-ex4.yml ",
    "test.sauce.ex4.us": "CURRENT_TIME=$(date +%s) saucectl run -c ./.sauce/config-ex4.yml --region us-west-1",
    "test.sauce.ex5.eu": "CURRENT_TIME=$(date +%s) saucectl run -c ./.sauce/config-ex5.yml ",
    "test.sauce.ex5.us": "CURRENT_TIME=$(date +%s) saucectl run -c .sauce/config-ex5.yml --region us-west-1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^9.6.0",
    "eslint": "^8.15.0",
    "saucectl": "^0.95.0"
  }
}
